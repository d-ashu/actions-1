name: My First workflow

on: push


jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "my 1st github action Job"

        - name: list files
          run: ls

        - name: Read Files
          run: cat README.md
          

